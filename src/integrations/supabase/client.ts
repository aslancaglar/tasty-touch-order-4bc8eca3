
// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';
import { SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, SESSION_EXPIRY_SECONDS, AUTH_STORAGE_KEY } from '@/config/supabase';

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

export const supabase = createClient<Database>(
  SUPABASE_URL, 
  SUPABASE_PUBLISHABLE_KEY,
  {
    auth: {
      autoRefreshToken: true,
      persistSession: true, 
      storage: localStorage,
      storageKey: AUTH_STORAGE_KEY,
      // Detect when auth sessions expire or are invalidated
      detectSessionInUrl: true,
    },
    global: {
      // Add retries for improved resilience
      headers: { 'x-application-name': 'qimbo-kiosk' },
    },
  }
);

